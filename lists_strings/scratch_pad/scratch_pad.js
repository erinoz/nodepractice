inputData = 


for (let i = 0; i < *insert number of array objects here*; i++)
  if (email = ssantiago@netflix.com) {
    return response
  } else {
  }




  for (let i = 0; i < *insert number of array objects here*; i++)
  if (email = ssantiago@netflix.com) {
    return response
  } else {
  }


const attendeesArray = inputData.attendeesArray.split(",");
const filteredAttendee = attendeesArray.filter(attendee => attendee.includes('ssantiago@netflix.com'));

const attendeesArray = inputData.attendeesEmailArray.length;

for (let i = 0; i < numberOfEmails; i++)
  if (attendeesEmailArray[i] === ssantiago@netflix.com && attendeesResponses === "accepted") {
    const santiagoAttending = true;
  } else {
  }; 

output = [{attendeesEmailArray: attendeesEmailArray, number_of_emails: numberOfEmails, santiago_attending: santiagoAttending}];

// 

var attendeesEmailArray = inputData.attendeesEmails.split(','); 
var attendeesResponsesArray = inputData.attendeesResponses.split(','); 
var numberOfEmails = attendeesEmailArray.length; 
var santiagoAttending = 'didnt check yet'; 

var santiagoPosition = attendeesEmailArray.indexOf("ssantiago@netflix.com");
console.log(santiagoPosition)

var santiagoResponse = attendeesResponsesArray[santiagoPosition]
console.log(santiagoResponse)

//for (var i = 0; i < numberOfEmails; i++) {
  //if (numberOfEmails < 10) {
    //var santiagoAttending = 'correct';
 // } else {
    //var santiagoAttending = 'fail';
 // }
//}

// output = [{attendeesEmailArray: attendeesEmailArray, number_of_emails: numberOfEmails, santiago_attending: santiagoAttending}];*/

output = [{attendeesEmailArray: attendeesEmailArray, number_of_emails: numberOfEmails, attendee_responses: attendeesResponsesArray, santiago_response: santiagoResponse}];